# 📝 To-Do App

A simple and efficient **To-Do application** built using **Node.js**, **Express.js**, **PostgreSQL**, and **EJS**. This app allows users to create, view, update, and delete tasks with persistent storage using a relational database.
